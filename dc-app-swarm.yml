version: '3'
networks:
  default:
    external:
      name: proto1_swarm

services:
  gateway:
    image: gateway:jvm
    deploy:
      replicas: 1
    ports:
      - "8080:8080"

  order-microservice:
    image: order-microservice:jvm
    deploy:
      replicas: 1



